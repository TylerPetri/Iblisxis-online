export enum GameActions {
  waiting = 'waiting',
  start = 'start',
  yourTurn = 'yourTurn',
  opponentTurn = 'opponentTurn'
}

export enum Direction {
  horizontal = 'horizontal',
  vertical = 'vertical'
}

export enum Player {
  ally = 'ally',
  enemy = 'enemy'
}